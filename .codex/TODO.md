# Repo TODO / Open Questions

- Confirm whether tests should be introduced and, if so, decide test framework and command.
- Confirm whether a lint command beyond `cargo fmt` should be enforced.
- If deployment diverges from the repo-name Pages URL, update the `public_url` workflow command and docs together.
